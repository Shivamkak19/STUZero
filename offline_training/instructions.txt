To run the offline training, follow these steps:

1. Make sure ezv2 environment is configured as instructed in INSTALL.md.
2. Download dataset into offline_training/ folder from Hugging Face by passing download flag to offline.sh script

pip install huggingface_hub     

sh offline.sh pong baseline --download

3. Run dynamics training with baseline:
sh offline.sh pong baseline

4. Run dynamics training with use_stu flag:
sh offline.sh pong stu